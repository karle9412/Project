<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">

    <select id="BoardPaging"
            resultType="com.project.board.vo.BoardVo">
        SELECT *
          FROM (
             SELECT ROW_NUMBER() OVER (ORDER BY BOARD_NUMBER ASC) RNUM,
                    C.*
                   FROM CUSTOMER_BOARD C
               ORDER BY BOARD_NUMBER ASC)
         WHERE RNUM BETWEEN #{pageNum} AND #{contentNum}
    </select>

    <select id="BoardCount"
            resultType="int">
        SELECT COUNT(*)
          FROM CUSTOMER_BOARD
    </select>

    <select id="CustomerBoardList"
            resultType="com.project.board.vo.BoardVo">
        SELECT BOARD_NUMBER,
        TITLE,
        MENU_ID,
        WRITER,
        BOARD_LOCAL,
        CONT,
        TO_CHAR(INDATE + 9/24, 'yyyy-mm-dd hh24:mi:ss') AS INDATE,
        BOARD_CHECK

        FROM CUSTOMER_BOARD

        WHERE MENU_ID = #{menu_id}


        ORDER BY BOARD_NUMBER DESC

    </select>

    <select id="RiderBoardList"
            resultType="com.project.board.vo.RiderBoardVo">
        SELECT BOARD_NUMBER,
        TITLE,
        MENU_ID,
        WRITER,
        RIDER_LOCAL,
        CONT,
        TO_CHAR(INDATE + 9/24, 'yyyy-mm-dd hh24:mi:ss') AS INDATE,
        BOARD_CHECK

        FROM RIDER_BOARD

        WHERE MENU_ID = #{menu_id}


        ORDER BY BOARD_NUMBER DESC

    </select>

    <insert id="C_InsertBoard" parameterType="com.project.board.vo.BoardVo">
        INSERT INTO CUSTOMER_BOARD (BOARD_NUMBER,MENU_ID,TITLE,WRITER,CONT)
        VALUES(BOARD_NUMBER.NEXTVAL,#{menu_id},#{title},#{writer},#{cont})


    </insert>

    <insert id="R_InsertBoard" parameterType="com.project.board.vo.RiderBoardVo">
        INSERT INTO RIDER_BOARD (BOARD_NUMBER,MENU_ID,TITLE,WRITER,CONT)
        VALUES(BOARD_NUMBER.NEXTVAL,#{menu_id},#{title},#{writer},#{cont})


    </insert>

    <select id            = "DetailCustomerBoard"
            parameterType = "hashmap"
            resultType    = "com.project.board.vo.BoardVo">
        SELECT BOARD_NUMBER,
        TITLE,
        WRITER,
        BOARD_LOCAL,
        CONT,
        TO_CHAR(INDATE + 9/24, 'yyyy-mm-dd hh24:mi:ss') AS INDATE


        FROM CUSTOMER_BOARD


        where board_number = #{board_number}
    </select>

    <select id            = "DetailRiderBoard"
            parameterType = "hashmap"
            resultType    = "com.project.board.vo.RiderBoardVo">
        SELECT BOARD_NUMBER,
        TITLE,
        WRITER,
        RIDER_LOCAL,
        CONT,
        TO_CHAR(INDATE + 9/24, 'yyyy-mm-dd hh24:mi:ss') AS INDATE

        FROM RIDER_BOARD

        where board_number = #{board_number}
    </select>


</mapper>