<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="File">

    <!-- db에 유저 아이디가 있는 지-->
    <select id="getUserProfile"
            resultType="String">
        SELECT USERID
        FROM USER_FILE
        WHERE USERID = #{vo.userid}
    </select>

    <!-- db에 rider_reply_number가 있는지-->
    <select id="getReplyFile"
            resultType="String">
        SELECT RIDER_REPLY_NUMBER
        FROM RIDER_REPLY_FILE
        WHERE RIDER_REPLY_NUMBER = #{rider_reply_number}
    </select>


    <!-- 유저 상세화면에서 지정된 화면을 불러들이기 위함 -->
    <select id="getProfile"
            resultType="com.project.pds.user.vo.FilesVo">
        SELECT SFILENAME
        FROM USER_FILE
        WHERE USERID=#{userid}
    </select>

    <!-- db에 데이터를 넣는 -->
    <insert id="insertUserProfile">
        INSERT  INTO  USER_FILE(
        USERID,
        FILENAME,
        FILEEXIT,
        SFILNAME)
        VALUES (#{vo.userid}, #{vo.fileName}, #{vo.fileExit}, #{vo.sFileName})
    </insert>

    <insert id="insertReplyFile">
        INSERT  INTO  RIDER_REPLY_FILE(
        FILE_NUM,
        RIDER_REPLY_NUMBER,
        FILENAME,
        FILEEXIT,
        SFILENAME)
        VALUES (RIDER_REPLY_NUMBER.NEXTVAL, #{rider_reply_number}, #{vo.fileName}, #{vo.fileExit}, #{vo.sFileName})

    </insert>

    <!-- db에 있는 데이터를 파일 데이터를 수정 -->
        <update id="updateUserProfile">
            UPDATE USER_FILE
            SET
            FILENAME=#{vo.fileName},
            FILEEXIT=#{vo.fileExit},
            SFILENAME=#{vo.sFileName}

            WHERE USERID=#{vo.userid}

        </update>

    <update id="updateReplyFile">
        UPDATE RIDER_REPLY_FILE
        SET
        FILENAME=#{vo.fileName},
        FILEEXIT=#{vo.fileExit},
        SFILENAME=#{vo.sFileName}

        WHERE RIDER_REPLY_NUMBER = #{rider_reply_number}

    </update>
</mapper>