<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">

    <select id="BoardList"
            resultType="com.project.board.vo.BoardVo">
        SELECT BOARD_NUMBER,
        TITLE,
        WRITER,
        BOARD_LOCAL,
        CONT,
        TO_CHAR(INDATE + 9/24, 'yyyy-mm-dd hh24:mi:ss') AS INDATE

        FROM BOARD

        WHERE
         MENU_ID = #{menu_id}

        ORDER BY BOARD_NUMBER DESC

    </select>

    <insert id="InsertBoard" parameterType="com.project.board.vo.BoardVo">
        INSERT INTO BOARD (BOARD_NUMBER,MENU_ID,TITLE,WRITER,CONT)
        VALUES(BOARD_NUMBER.NEXTVAL,#{menu_id},#{title},#{writer},#{cont})

    </insert>

    <select id            = "Board"
            parameterType = "hashmap"
            resultType    = "com.project.board.vo.BoardVo">
        SELECT BOARD_NUMBER,
        TITLE,
        WRITER,
        BOARD_LOCAL,
        CONT,
        TO_CHAR(INDATE + 9/24, 'yyyy-mm-dd hh24:mi:ss') AS INDATE

        from board

        where board_number = #{board_number}
    </select>


</mapper>